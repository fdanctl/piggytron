package partials

import (
	"fmt"
	"github.com/fdanctl/piggytron/web/templates/components"
	"github.com/fdanctl/piggytron/web/views"
)

templ IncomeCategoryForm(id string, v views.IncomeCategoryForm) {
	<form
		class="space-y-sm"
		hx-on:submit="event.preventDefault()"
		if id == "" {
			hx-post="/partials/income-category"
		} else {
			hx-post={ fmt.Sprintf("/partials/income-category/%s", id) }
		}
		hx-swap="outerHTML"
		novalidate
	>
		@components.InputGroup(
			"",
			"Name",
			views.StringArrToStr(v.ValidateName()),
			v.NameHasError(),
			true,
			templ.Attributes{
				"id":          "name",
				"type":        "text",
				"name":        "name",
				"placeholder": "Salary",
				"value":       v.Name,
			},
		)
		<div class="w-full flex justify-end mt-md">
			@components.Button(
				"Enter",
				"mt-md",
				components.BtnPrimary,
				components.BtnMedium,
				templ.Attributes{
					"type": "submit",
				},
			)
		</div>
	</form>
}
